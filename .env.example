# ------------------------------
# Finance Index Dashboard - Env
# ------------------------------

# Database connection (for local Docker Compose)
DATABASE_URL=postgresql+psycopg://postgres:postgres@db:5432/finance

# API key for Finnhub (optional — used if you switch from yfinance)
#FINNHUB_API_KEY=your_key_if_you_switch_from_yfinance

# Frontend will use this to call the backend API
NEXT_PUBLIC_API_URL=http://localhost:8000

# (Optional) If you later deploy to GCP, override DATABASE_URL with your Cloud SQL socket connection path:
# DATABASE_URL=postgresql+psycopg://<USER>:<PASS>@/<DB>?host=/cloudsql/<PROJECT>:<REGION>:<INSTANCE>

# (Optional) Set your timezone for ETL cron jobs
TZ=UTC

